require 'spec_helper'

describe Rack::Downcase do
  it 'has a version number' do
    expect(Rack::Downcase::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
